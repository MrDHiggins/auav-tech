import ContactFormWrapper from '@/app/ui/contact/contactFormWrapper';

export default function ContactPage() {
    return (
        <div>
            <h1>Contact Page</h1>

            <ContactFormWrapper/>
        </div>
    );
}